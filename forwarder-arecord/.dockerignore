# Build context here is intentionally tiny (Dockerfile + entrypoint.sh).
# Ignore lists are purely defensive against future additions.

# Editor / OS noise.
.DS_Store
*.swp
*~

# Docs / VCS — should never end up inside the runtime image.
*.md
.git
.gitignore
.gitattributes
